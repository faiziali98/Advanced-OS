Folders: 
	1. Parent directory contain server code and client(which was not used) written in c language.
	2. Client directory contain client code.
	3. Test_files contain 10 files of 13.6mb each

Usage:
	1. Run ./server command it will tell you its usage
	2. Client can be run using run-time parameters but we used different versions of client.py (available in directories).

How we used these files is mentioned in our report
